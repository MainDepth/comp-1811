# comp-1811

The main application is the GUI.py file, to see how the app runs. 
In the UTIL.py files are all the database handling functions

Most of the functions are used from the UTIL.py module.

passInfoIntoDatabase.py is just a test script (Do not run)
